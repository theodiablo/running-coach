name: Regenerate OG image

# Keeps the social-share cards (public/og-image.png for site links,
# public/og-watch.png for /watch/ run links) in sync with the marketing copy.
# Triggers only when the copy or a card template changes, renders fresh 1200x630
# PNGs with headless Chrome, and commits them back to the branch.
#
# It runs on feature branches (not main), so by the time a copy change reaches
# main the refreshed image is already committed and the normal S3/CloudFront
# deploy uploads them. The commit it pushes only touches the two PNGs, which are
# NOT in the paths filter below, so it can never retrigger this workflow.
on:
  push:
    branches-ignore: [main]
    paths:
      - src/marketing/copy.json
      - scripts/og-image/**

# Only the latest run per branch needs to win.
concurrency:
  group: og-image-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          # Node 22 to match every other workflow. No cache: npm here — this
          # job never runs npm ci (generate.mjs is dependency-free).
          node-version: 22

      # ubuntu-latest ships Google Chrome; install it only if that ever changes.
      - name: Ensure Chrome
        run: |
          if ! command -v google-chrome >/dev/null 2>&1; then
            wget -q -O /tmp/chrome.deb https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
            sudo apt-get update && sudo apt-get install -y /tmp/chrome.deb
          fi
          echo "CHROME_BIN=$(command -v google-chrome)" >> "$GITHUB_ENV"

      - name: Regenerate social cards
        run: node scripts/og-image/generate.mjs

      - name: Commit if changed
        run: |
          if [ -n "$(git status --porcelain public/og-image.png public/og-watch.png)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add public/og-image.png public/og-watch.png
            git commit -m "chore: regenerate social cards from marketing copy"
            git push
            echo "Social cards updated and pushed."
          else
            echo "Social cards already up to date."
          fi
